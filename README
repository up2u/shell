1:steps of push
	mkdir shell
	cd shell
	git init
	touch README
	git add README
	git commit -m 'first commit'
	git remote add origin git@github.com:up2u/shell.git
	git push origin master
2:files.
	1): clean.sh 
	2): find.sh
		find -name  with -path -prune to avoid some specified directory. 
	3): fork_source_exec.sh, usage: ./fork_source_exec.sh {source | exec | fork} 
	    for_fork_source_exec.sh
	4): parenthese_brace_single.sh
		just include command group to test they difference.
		> (command1, command2, command3..) ; 
		> function: {
					command1, command2, command3..
				    }; # NOTE: the commands must't in same line with rear }, or will error
	5): $@_$*.sh : difference between $@, $*
	6): parenthese_brace_with_$.sh
		> most of operation parenthese brace with $.
	7): file.txt
		low2cap.sh : change 1st character of every word in the file.txt to CAP case.
	8): awk.sh
others:
